---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.2
build:
  requirementsPath: requirements.txt
run:
  scriptName: gunicorn user_test/user_test/wsgi.py:application --bind 0.0.0.0:80
  persistenceMount: /data
  containerPort: 80
