meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.2
build:
  requirementsPath: requirements.txt
run:
  scriptName: cd user_test && gunicorn project.wsgi:application --bind 0.0.0.0:80
  persistenceMount: /data
  containerPort: 80
