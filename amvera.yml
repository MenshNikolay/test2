---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.2
build:
  requirementsPath: requirements.txt
run:
  persistenceMount: /data
  containerPort: 80
  command: "gunicorn user_test.user_test,wsgi:application --bind 0.0.0.0:80"
