---
meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.2
build:
  requirementsPath: requirements.txt
run:
  scriptName: 
  persistenceMount: /data
  containerPort: 80
  command: cd user_test && gunicorn user_test.wsgi:application --bind 0.0.0.0:80
